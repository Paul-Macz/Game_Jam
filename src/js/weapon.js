
export default class Weapon{
    constructor(scene, name, damage, atSpeed, userSpeed, image, byPlayer){
        this.scene=scene;
        this.name=name;
        this.damage=damage;
        this.atSpeed=atSpeed;
        this.userSpeed=userSpeed;
        this.image=image;
        this.byPlayer=byPlayer;
    }

    attack(){

    }
}